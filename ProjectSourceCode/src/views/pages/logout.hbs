<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Logout</title>
    <style>
        /* General Body Styling */
        body {
            font-family: 'Arial', sans-serif;
            background-color: #D9E3F0;
            /* Soft blue background */
            color: #333;
            display: flex;
            align-items: center;
            height: 100vh;
            margin: 0;
            padding: 0;
            background-image: url('https://aboutboulder.com/wp-content/uploads/2023/08/The-Vibrant-Campus-Life-at-CU-Boulder-scaled.jpeg');
            /* Subtle gradient background */
            background-size: cover;
            background-position: center;
        }

        /* Header Styling */
        h1 {
            color: #fff;
            font-size: 3rem;
            font-weight: bold;
            text-align: center;
            margin-bottom: 30px;
            text-shadow: 2px 2px 8px rgba(0, 0, 0, 0.3);
            /* Slight shadow effect */
        }

        .login-form:hover {
            transform: scale(1.05);
            /* Slightly enlarge form on hover */
        }

        /* Form Group Styling */
        .form-group {
            margin-bottom: 20px;
        }

        /* Input Fields Styling */
        .form-control {
            width: 100%;
            padding: 12px;
            font-size: 1rem;
            border: 2px solid #B5D0E7;
            border-radius: 8px;
            margin-top: 5px;
            box-sizing: border-box;
            transition: border 0.3s ease-in-out;
        }

        .form-control:focus {
            outline: none;
            border-color: #007bff;
            box-shadow: 0 0 5px rgba(0, 123, 255, 0.5);
        }

        input::placeholder {
            color: #888;
        }

        /* Button Styling */
        .btn-primary {
            width: 20%;
            background-color: #007bff;
            border: none;
            padding: 14px;
            color: white;
            font-size: 1.2rem;
            border-radius: 5px;
            cursor: pointer;
            transition: background-color 0.3s ease-in-out;
            font-weight: bold;

            display: block;
            /* Make the button block-level */
            margin: 0 auto;
            /* Center it horizontally */
        }


        .btn-primary:hover {
            background-color: #0056b3;
        }

        /* Responsive Design */
        @media (max-width: 600px) {
            .login-form {
                padding: 20px;
                max-width: 80%;
            }

            h1 {
                font-size: 2.5rem;
            }

            .btn-primary {
                font-size: 1rem;
            }
        }
    </style>
</head>

<body>

    <div class="container">
        <section id="auth-links" style="margin-top: 20px;">
            <h1>Goodbye!</h1>
            <form action="/logout" method="POST">
                <button type="submit" class="btn-primary">Log back in?</button>
            </form>
    </div>
</body>

</html>